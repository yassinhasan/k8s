apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vscode-gcloud-pvc
  namespace: homelab
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: ""
  volumeName: vscode-gcloud-pv