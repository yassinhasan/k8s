apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vscode-workspace-pvc
  namespace: homelab
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: ""
  volumeName: vscode-workspace-pv